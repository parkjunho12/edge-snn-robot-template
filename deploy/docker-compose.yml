services:
  edge-snn-robot:
    build:
      context: .                 # 컨텍스트는 리포 루트
    image: edge-snn-robot:dev
    ports: ["8000:8000"]
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1

